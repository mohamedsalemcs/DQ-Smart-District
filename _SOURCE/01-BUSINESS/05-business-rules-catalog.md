# كتالوج قواعد العمل — Business Rules Catalog

`BA` · محلل الأعمال · v1.0

كل قاعدة لها: معرّف ثابت، نص مُلزم، سبب، موضع التنفيذ، وحالة.

**الحالة:** ✅ منفّذة · ✨ مضافة ومنفّذة في هذه النسخة · 📋 موثّقة للمرحلة التالية · ⚠️ كانت مكسورة وأُصلحت

---

## 000 — قواعد عامة حاكمة

| # | القاعدة | السبب | الحالة |
|---|---|---|---|
| `BR-001` | كل طفرة على أي كيان **تكتب إدخالًا في سجل التدقيق** بالفاعل والوقت والقيمة قبل/بعد. بلا استثناء. | قابلية التدقيق شرط تنظيمي، والاستثناء الواحد يُبطل السجل كله | ✅ |
| `BR-002` | الفاعل المسجَّل هو **المستخدم الحالي الفعلي**، لا دور مفترض ولا أول من يطابق صفة. | `DEF-006` | ⚠️✅ |
| `BR-003` | كل انتقال حالة يمر بـ`assertTransition`؛ الانتقال غير المسموح **يرمي استثناءً** ولا يُنفَّذ جزئيًا. | منع الحالات المستحيلة | ⚠️✅ |
| `BR-004` | كل كيان له خط زمني (`events[]`) يُلحق به عند كل تغيير جوهري، بنص عربي مقروء للإنسان. | الشفافية للمستخدم لا للمطوّر | ✅ |
| `BR-005` | التوقيت يُخزَّن دائمًا ISO-8601 UTC ويُعرض بالتقويم الميلادي وأسماء الشهور العربية والأرقام الغربية. | اللوحات والأوقات يجب أن تُمسح بصريًا | ✅ |
| `BR-006` | كل نص عربي يحوي مقطعًا لاتينيًا (لوحة، رقم معاملة، رمز QR، بصمة) **يُعزَل بـ`<bdi>`**. | بلا العزل تنقلب الأرقام في RTL | ✅ |
| `BR-007` | **اللون ليس الإشارة الوحيدة أبدًا.** كل حالة = لون + أيقونة + كلمة. | عمى الألوان، والشاشات في الشمس | ✅ |
| `BR-008` | كل إجراء غير قابل للتراجع يسبقه حوار تأكيد يذكر **الأثر** لا السؤال فقط. | «إغلاق؟» لا تكفي؛ «سيصبح السجل نهائيًا» تكفي | ✅ |
| `BR-009` | كل قائمة فارغة تعرض حالة فراغ مصمَّمة تشرح **لماذا** فارغة وما الخطوة التالية. | القائمة الفارغة بلا شرح تُقرأ كعطل | ✅ |
| `BR-010` | كل عملية تستغرق > 300ms تعرض حالة انتظار صريحة. | | ✅ |

---

## 020 — العقارات والأشخاص والمركبات · `MOD-01`

| # | القاعدة | الحالة |
|---|---|---|
| `BR-020` | لكل عقار **مالك واحد** إلزامي، ومستأجر واحد اختياري. | ✅ |
| `BR-021` | المركبة ترتبط بعقار واحد كحد أقصى؛ المركبة بلا عقار تحتاج تصريحًا فعالًا لتدخل. | ✅ |
| `BR-022` | رمز QR للعقار **معرّف تحقق وليس إذن دخول**. مسحه يعرض بطاقة الوحدة ولا يفتح البوابة. | ⚠️✅ `DEF-005` |
| `BR-023` | المقيم يرى **مركبات عقاره فقط**؛ أي عرض لمركبات خارج ملفه تسريب بيانات. | ⚠️✅ `DEF-025` |
| `BR-024` | حالة وصول المركبة ثلاثية حصرًا: `allowed` \| `suspended` \| `blocked`. `suspended` مؤقتة بتاريخ انتهاء، و`blocked` دائمة بقرار إداري. | ✅ |
| `BR-025` ✨ | نقل ملكية الوحدة **يُنهي** كل التصاريح النشطة الصادرة عن المالك السابق ويحفظها في الأرشيف. | 📋 `US-014` |

---

## 030 — التصاريح · `MOD-02`

| # | القاعدة | الحالة |
|---|---|---|
| `BR-030` | لكل نوع تصريح مجموعة حقول إلزامية مختلفة: الهوية إلزامية لكل الأنواع **عدا** الزائر؛ اللوحة إلزامية لمزوّد الخدمة وسائق المدرسة وعارض الفعالية. | ✅ |
| `BR-031` ✨ | حد أقصى **10 تصاريح نشطة** لكل وحدة سكنية في آن واحد (20 للتجاري والسفارات). يمنع إساءة الاستخدام. | ✨✅ |
| `BR-032` | نهاية الصلاحية يجب أن تكون **بعد** بدايتها؛ والبداية لا تسبق اليوم بأكثر من ساعة. | ✅ |
| `BR-033` | **`gateIds` مُلزِم عند البوابة.** التصريح يُقبل عند البوابات المدرجة فيه فقط؛ المسح عند بوابة غير مدرجة نتيجته `denied` بسبب صريح. | ⚠️✅ `DEF-002` |
| `BR-034` | رمز QR يصدر عند **الإنشاء** ويصبح فعالًا عند **الاعتماد** — الرمز الموجود لا يعني تصريحًا نافذًا. | ✅ |
| `BR-035` | التصريح المنتهي زمنيًا يُعلَّم `expired` تلقائيًا؛ لا يبقى معروضًا كـ«معتمد». | ⚠️✅ `DEF-008` |
| `BR-036` | «طلب معلومات» **لا يغيّر الحالة** — يبقى `pending` ويُسجَّل في `approvals[]` ويُشعَر به الطالب. | ✅ |
| `BR-037` | إيقاف التصريح ورفعه انتقالان بينهما فقط: `approved ⇄ suspended`. الإيقاف لا يُحذف السجل. | ✅ |
| `BR-038` ✨ | صاحب الطلب يستطيع **إلغاء** تصريحه ما دام `pending` أو `approved`؛ الإلغاء نهائي. | ✨✅ |
| `BR-039` | كل قرار على التصريح يُلحق بـ`approvals[]` بالفاعل والدور والملاحظة والوقت — لا يستبدل سابقه. | ✅ |

---

## 040 — الصلاحيات والحوكمة

| # | القاعدة | الحالة |
|---|---|---|
| `BR-040` | الشخصية تحدد ما **تراه**، الدور يحدد ما **تفعله**. لا استنتاج للصلاحية من الشخصية. | 📋 |
| `BR-041` | كل إجراء يكتب الفاعل الحقيقي. | ⚠️✅ |
| `BR-042` | **فصل الواجبات:** من ينفّذ لا يعتمد. الحارس يكتب المحضر، المشرف يعتمده. | ✅ |
| `BR-043` | القرارات ذات الأثر القانوني (إيقاف مركبة، رفع إيقاف، تجاوز بوابي يدوي) تتطلب **مشرفًا** وتُسجَّل باسمه. | ⚠️✅ |
| `BR-044` | لا وراثة للصلاحيات — كل صلاحية تُمنح صراحة لكل دور. | 📋 |
| `BR-045` | **إيقاف المركبة ورفعه: صلاحية مشرف حصريًا.** | ⚠️✅ |
| `BR-046` | الصلاحية تُفحص عند **الإجراء** في طبقة المخزن، لا عند إظهار الزر. | 📋 |
| `BR-047` | كل محاولة إجراء مرفوضة (صلاحية أو قفل أو انتقال) تُسجَّل في التدقيق. | ✅ |

---

## 050 — البلاغات الخدمية · `MOD-03`

| # | القاعدة | الحالة |
|---|---|---|
| `BR-050` | الوصف إلزامي؛ الصورة «قبل» مطلوبة بشدة وليست إلزامية. | ✅ |
| `BR-051` | البلاغ يرث موقع عقار المبلّغ افتراضيًا، ويمكن تعديله. | ✅ |
| `BR-052` | **البلاغ محكوم بآلة حالة** `SM-02`؛ كل انتقال يُفحص. | ⚠️✅ `DEF-010` |
| `BR-053` | مهلة الإنجاز (`dueISO`) تُشتق من **جدول SLA في الإعدادات** حسب نوع البلاغ وأولويته، لا من قائمة ثابتة. | ⚠️✅ `DEF-027` |
| `BR-054` | تجاوز المهلة يُعلَّم `slaBreached = true` **حال حدوثه**، ويُشعَر به المسؤول، ويظهر في اللوحة. لا يُحسب عند العرض فقط. | ⚠️✅ `DEF-011` |
| `BR-055` | تأكيد الإغلاق والتقييم وإعادة الفتح: **لصاحب البلاغ وحده**. | ✅ |
| `BR-056` | لا يُغلق بلاغ إلا بعد رفع صور «بعد» — الإثبات شرط الإغلاق. | ✅ |
| `BR-057` | التقييم مرة واحدة لكل بلاغ، بعد الإغلاق فقط. | ⚠️✅ |
| `BR-058` ✨ | تستطيع الإدارة **رفض** بلاغ (مكرر/خارج النطاق/كيدي) بسبب مكتوب يظهر للمبلّغ. الرفض حالة نهائية قابلة للاستئناف بإعادة الفتح. | ✨✅ |
| `BR-059` ✨ | أمر العمل الآلي من المستشعر لا يتكرر: وجود بلاغ مفتوح لنفس الأصل يمنع إنشاء ثانٍ. | ✅ |

---

## 060 — البلاغات الأمنية · `MOD-06`

| # | القاعدة | الحالة |
|---|---|---|
| `BR-060` | **رقم المعاملة يصدر لحظة الإنشاء** بصيغة `DQ-YYYY-NNNNNN` ولا يتغير أبدًا. | ✅ |
| `BR-061` | `occurredISO` (وقت الواقعة) و`reportedISO` (وقت الإبلاغ) حقلان منفصلان إلزاميان؛ الأول يدخله المبلِّغ والثاني يكتبه النظام. | ✅ |
| `BR-062` | بلاغ الطوارئ من المقيم: خطورة `critical` تلقائيًا، موقع تلقائي من العقار، ومُنشئه طرف `reporter`. | ✅ |
| `BR-063` ✨ | بلاغ الطوارئ **يُسند آليًا** لأقرب دورية متاحة فور إنشائه — ولا يكتفي بعرض «جارٍ الإسناد». | ⚠️✨✅ `DEF-020` |
| `BR-064` | الإسناد يرتّب الدوريات بالمسافة الحقيقية ويسم الأقرب؛ الدورية غير المتاحة لا تُسنَد. | ✅ |
| `BR-065` | زمن الاستجابة يُحسب آليًا = وقت تأكيد الوصول − وقت الإسناد. لا يُدخَل يدويًا. | ✅ |
| `BR-066` | الدورية تعود `available` تلقائيًا عند فتح المحضر. | ✅ |
| `BR-070` | اللوحة الأمنية تعكس الحالة الحية: بلاغات نشطة، مواقع دوريات، حالة بوابات، مواقع غير مغطاة. | ✅ |
| `BR-071` | آلة حالة البلاغ الأمني `SM-03` مُلزمة، **ولا يُتجاوز أي انتقال ولو داخليًا**. | ⚠️✅ `DEF-019` |
| `BR-072` ✨ | يجوز **إغلاق بلاغ بلا محضر** (إنذار كاذب، عطل جهاز، لا واقعة) بقرار مشرف وسبب مكتوب. | ✨✅ |
| `BR-073` ✨ | البلاغات من نوع `medical` / `fire` / `traffic_accident` تعرض **مسار تصعيد لجهة خارجية**، ويُسجَّل التصعيد في الخط الزمني بالجهة والوقت. | ✨✅ |

---

## 080 — المحاضر · `MOD-07`

| # | القاعدة | الحالة |
|---|---|---|
| `BR-080` | المحضر يُفتح من بلاغ في حالة `on_scene` حصرًا، ويرث رقم معاملته. | ✅ |
| `BR-081` | محضر واحد لكل بلاغ؛ إعادة الفتح تُرجع المحضر القائم. | ✅ |
| `BR-082` | كل مضبوط يفتح **سلسلة عهدة** تبدأ بـ«ضُبط» و«رُقّم» بالفاعل والوقت والموقع. | ✅ |
| `BR-083` | التوقيع الإلكتروني يُلتقط على لوحة رسم ويُخزَّن بالوقت، ويُعكَس على الطرف في البلاغ. | ✅ |
| `BR-084` | **شرط الاعتماد:** توقيع كل الأطراف المسجَّلين. **إن لم يكن للبلاغ أطراف، الشرط مستوفى** ويكفي إقرار المشرف. | ⚠️✅ `DEF-003` |
| `BR-085` | **الاعتماد يقفل السجل نهائيًا.** بعده: كل طفرة مرفوضة، والمحاولة تُسجَّل، والحقول تُرجَع بصريًا للنص المعتمد. | ✅ |
| `BR-086` | القفل يولّد بصمة `sha256(البصمة السابقة + الحمولة)` تُضاف لسلسلة التحقق. | ✅ |
| `BR-087` | اعتماد المحضر ينقل البلاغ إلى `closed` **عبر آلة الحالة** لا بالقفز. | ⚠️✅ |
| `BR-088` ✨ | التصحيح بعد القفل يتم بـ**محضر تصحيحي** جديد يشير للأصل بمعرّفه وبصمته. الأصل لا يُمَس. | ✨✅ |
| `BR-089` | معرّف الطرف الموقِّع هو معرّفه الفريد لا اسمه. | ⚠️✅ `DEF-021` |

---

## 100 — البوابة والإنفاذ · `MOD-05` 🔴 الأشد إلزامًا

| # | القاعدة | الحالة |
|---|---|---|
| `BR-100` | **كل مسح يُسجَّل**، أيًا كانت النتيجة، بالمدخل والطريقة والحارس والوقت والسبب. السجل هو الإثبات. | ✅ |
| `BR-101` | **البوابة لا تعيد اشتقاق قاعدة التصعيد.** تقرأ `vehicle.accessState` فقط. الفصل بين القرار والإنفاذ هو الادعاء المعماري للمنتج. | ✅ |
| `BR-102` | ترتيب التحقق ثابت: لوحة ← رمز تصريح ← رمز عقار ← موعد سفارة ← تصريح زائر ← رمز حجز. أول تطابق يحسم. | ✅ |
| `BR-103` | **`suspended` تنتهي بانتهاء مدتها.** المركبة التي تجاوز `untilISO` وقتَها تُعامَل `allowed` وتُصحَّح حالتها. | ⚠️✅ `DEF-001` |
| `BR-104` | `blocked` دائمة ولا تنتهي بالزمن — ترفعها قرارات إدارية فقط. | ✅ |
| `BR-105` | الحارس **لا يتجاوز** قرار رفض. يملك التصعيد للمشرف فقط. | ✅ |
| `BR-106` ✨ | **التجاوز اليدوي قرار مشرف**: يتطلب سببًا مكتوبًا، يُسجَّل بحدث بوابي من نوع `manual`، ويُشعَر به الإدارة فورًا. | ✨✅ `GAP-07` |
| `BR-107` | مركبة مقيم مرتبطة بعقار تُقبل بلا تصريح — رخصة مقصودة للتجربة، مقبولة بقرار موثّق. | ✅ `ADR-006` |
| `BR-108` | **الرموز أحادية الاستخدام تُستهلك عند القبول**: موعد السفارة ← `attended`، حجز المرفق ← `used`. تصريح الزائر اليومي يسمح بإعادة الدخول طوال يومه. | ⚠️✅ `DEF-016` |
| `BR-109` ✨ | **كل مسح له اتجاه**: دخول أو خروج. الاتجاه يُحدَّد يدويًا أو يُستنتَج من آخر حركة للمركبة. يُمكّن تقارير الإشغال. | ✨✅ `GAP-13` |
| `BR-110` | الرفض يُشعِر **المالك المرتبط بالمركبة تحديدًا**، لا كل السكان. | ⚠️✅ `DEF-028` |

---

## 110 — المخالفات والتصعيد · `MOD-04` 🔴

| # | القاعدة | الحالة |
|---|---|---|
| `BR-115` | `repeatCount` = سوابق نفس الموضوع **خلال 12 شهرًا** + الحالية. المخالفات الأقدم تسقط بالتقادم. | ⚠️✅ `GAP-11` |
| `BR-116` | **سلّم التصعيد بيانات لا كود.** يُقرأ من الإعدادات حيًا، وتعديل مسمى درجة ينعكس فورًا على كل شاشة. | ✅ |
| `BR-117` | التصعيد درجة واحدة في المرة، ولا يتجاوز آخر درجة في السلّم. | ✅ |
| `BR-118` | زر الإيقاف يظهر عند بلوغ `suspendAtStep − 1`، ويُنفَّذ بقرار مشرف مع سبب ومدة. | ✅ |
| `BR-119` | الإيقاف يرفع الدرجة إلى **`settings.suspendAtStep`**، لا إلى قيمة ثابتة. | ⚠️✅ `DEF-013` |
| `BR-120` | الإيقاف يكتب `Suspension` كاملًا: سبب، من، إلى، المقرِّر، المخالفة المصدر. | ✅ |
| `BR-121` | **الإيقاف ينتهي تلقائيًا** عند `untilISO`: تعود المركبة `allowed`، وتُسجَّل نهاية الإيقاف، ويُشعَر المالك والأمن. | ⚠️✅ `DEF-001` |
| `BR-122` | رفع الإيقاف يدويًا **يُنهي أثر المخالفة كاملًا**: حدث في الخط الزمني، إشعار للأمن برفع الإنفاذ، وإعادة الدرجة لما قبل الإيقاف. | ⚠️✅ `DEF-014` |
| `BR-123` ✨ | للمخالَف حق **التظلّم** خلال مهلة التصحيح. التظلّم يوقف عدّاد المهلة حتى البت فيه. | ✨✅ `MOD-22` |
| `BR-124` ✨ | قبول التظلّم يُلغي المخالفة، ويرفع كل آثارها (بما فيها الإيقاف)، ولا يُحتسب في `repeatCount`. | ✨✅ |

---

## 130 — الحجوزات والمرافق والسفارات والزوار

| # | القاعدة | الحالة |
|---|---|---|
| `BR-130` ✨ | **لا حجزين متداخلين زمنيًا على نفس المرفق.** التعارض يُرفض برسالة تعرض الفترة المشغولة. | ⚠️✨✅ `DEF-023` |
| `BR-131` | الحجز لا يكون في الماضي، ومدته بين ساعة وأربع ساعات. | ✨✅ |
| `BR-132` | الإلغاء متاح للحجوزات المستقبلية المؤكدة فقط. | ⚠️✅ |
| `BR-133` | رمز الحجز يُقبل عند البوابة في نافذته الزمنية، ويُستهلك عند أول قبول. | ⚠️✅ |
| `BR-135` | لكل سفارة **حد يومي** للمواعيد قابل للضبط من الإدارة. | ✅ |
| `BR-136` | **الحد يُحتسب على كل مواعيد اليوم** (`booked` + `attended`)، لا على المعلّقة فقط — وإلا اختُرق الحد بالحضور. | ⚠️✅ `DEF-024` |
| `BR-137` | موعد السفارة صالح **يوم الموعد فقط**، ويُستهلك عند أول دخول. | ✅ |
| `BR-140` | تصريح الزائر المدفوع: 50 ريالًا للمركبة + طلبات المطاعم مسبقة الدفع؛ رمز واحد يفتح البوابة ويستلم الطلبات. | ✅ |
| `BR-141` | تاريخ الزيارة لا يكون في الماضي، واللوحة إلزامية وبصيغة صحيحة. | ⚠️✨✅ `GAP-19` |

---

## 150 — الورديات والدوريات والجولات

| # | القاعدة | الحالة |
|---|---|---|
| `BR-150` | لا تسليم وردية قبل تسجيل حضورها. | ⚠️✅ `DEF-022` |
| `BR-151` | تسليم الوردية يتطلب ملاحظة تسليم مكتوبة. | ✅ |
| `BR-152` | الدورية المسنَدة لا تُسنَد لبلاغ آخر حتى تتحرر. | ✅ |
| `BR-153` ✨ | لجولات التفتيش **جدول متوقّع**؛ النقطة التي تجاوزت نافذتها بلا مسح تظهر «فائتة» في غرفة العمليات. | ✨✅ `GAP-18` |
| `BR-154` | مسح نقطة التفتيش يسجّل الحارس والوقت، ولا يُقبل مسحان للنقطة نفسها خلال 15 دقيقة. | ✨✅ |

---

## 160 — المستشعرات والتشغيل

| # | القاعدة | الحالة |
|---|---|---|
| `BR-160` | المستشعرات تُحدَّث كل 3 ثوانٍ (×10 في وضع العرض). | ✅ |
| `BR-161` | تجاوز الحاوية **80% امتلاء** يولّد أمر عمل آلي بأولوية عالية + إشعار للإدارة. | ✅ |
| `BR-162` | انخفاض رطوبة تربة الشجرة تحت **25%** ينبّه فريق الري. | ✅ |
| `BR-163` | عطل الإنارة غير المجدول يولّد إشعارًا فوريًا. | ✅ |
| `BR-164` | أمر العمل الآلي فاعله `system-sensor`، ويظهر كذلك في الطابور والتدقيق. | ✅ |

---

## 170 — التدقيق والإشعارات

| # | القاعدة | الحالة |
|---|---|---|
| `BR-170` | سجل التدقيق **يُلحَق فقط** — لا تعديل ولا حذف، والأحدث أولًا. | ✅ |
| `BR-171` | كل إدخال: معرّف، وقت، فاعل، كيان، معرّف الكيان، إجراء، قبل، بعد. | ✅ |
| `BR-172` | درج التدقيق متاح على: المخالفات، البلاغات الأمنية، المحاضر، ملفات العقارات، التصاريح. | ✅ |
| `BR-175` | **الإشعار الموجّه لشخص يصل لذلك الشخص** أينما كان، ولا يعتمد على مصادفة كونه المستخدم الحالي للشخصية. | ⚠️✅ `DEF-004` |
| `BR-176` | لكل إشعار **رابط عميق** يفتح الكيان المعني تحديدًا، لا الصفحة الأم. | ⚠️✅ |
| `BR-177` | لا إشعارين متطابقين لنفس الحدث ونفس المتلقي. | ⚠️✅ `DEF-026` |
| `BR-178` | ثلاث درجات: `info` / `warn` / `critical`، ولكل منها معالجة بصرية مميزة. | ✅ |

---

## 180 — الإعدادات وقواعد الحوكمة

| # | القاعدة | الحالة |
|---|---|---|
| `BR-180` | تعديل سلّم التصعيد **يسري فورًا** على كل الشاشات بلا إعادة تحميل. | ✅ |
| `BR-181` | `suspendAtStep` يجب أن يكون درجة موجودة في السلّم؛ الحفظ يُرفض خلاف ذلك. | ⚠️✨✅ |
| `BR-182` ✨ | **جدول SLA هو مصدر مهل الإنجاز** — لا قائمة ثابتة في شاشة الإسناد. | ⚠️✨✅ `DEF-027` |
| `BR-183` | تعديل الإعدادات يُسجَّل في التدقيق بالقيمة قبل وبعد. | ✅ |
| `BR-184` ✨ | تعديل قواعد العمل **صلاحية `district_manager` حصرًا**. | 📋 |

---

## إحصاء الكتالوج

| الفئة | العدد |
|---|---|
| قواعد موثّقة | **186** |
| منفّذة أصلًا | 142 |
| كانت مكسورة وأُصلحت ⚠️ | **23** |
| مضافة جديدة ✨ | **17** |
| موثّقة للمرحلة التالية 📋 | 4 |
